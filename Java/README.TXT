1.Restore the 'client.sql' database backup in a local
  MySQL server, open Wampserver and make sure that the 3
  services are running.
The assumed database credentials are:
  	  Server  : localhost
  	  Username: root
  	  Password: <no password>
2.Open NetBeans IDE with the Apache Tomcat application 
  server installed. Open the project 'Webteklek' and run it.
